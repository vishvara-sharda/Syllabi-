
  # Tree hierarchy diagram

  This is a code bundle for Tree hierarchy diagram. The original project is available at https://www.figma.com/design/3f9s5JB59arGNne0Ga4Sm6/Tree-hierarchy-diagram.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  